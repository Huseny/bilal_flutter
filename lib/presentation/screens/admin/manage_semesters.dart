import 'package:flutter/material.dart';

class ManageSemester extends StatelessWidget {
  const ManageSemester({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
